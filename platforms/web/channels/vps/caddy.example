{$DRYDOCK_HOSTNAME} {
	encode gzip zstd

	root * {$DRYDOCK_WEB_ROOT}

	@private path /package.json /.git/* /shipping/* /drydock-artifact.json /.drydock-channel
	handle @private {
		respond 404
	}

	file_server
}
